#include <bits/stdc++.h>
using namespace std;
 int main()
 {
 	list<int> l;
 	list<int> ::iterator it;
 	 int n,p; scanf("%d",&n);
     for (int i = 0; i < n; ++i)
 	{	 cin>>p;
 			l.push_back(p);
 	}
   		for ( it = l.begin(); it<l.end(); ++it)
   			cout<<"a";


   	




 	return 0;
 }